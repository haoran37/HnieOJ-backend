server:
  port: ${SERVER_PORT:8080}

sa-token:
  token-name: Authorization
  token-prefix: Bearer

hnieoj:
  auth-cache:
    ttl:
      roles: 24h
      permissions: 12h
      empty: 5m
      jitter: 10m
      local-fallback: 2m
